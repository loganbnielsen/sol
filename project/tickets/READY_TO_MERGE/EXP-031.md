---
id: EXP-031
type: ux-finding
severity: medium
source: project/audits/2026-09-08_ux_audit.md
branch: EXP-031/pluto-workspace-sync
worktree: /home/lbendtly/Code/sol-EXP-031-pluto-workspace-sync
pr: https://github.com/loganbnielsen/sol/pull/157
---

`examples/pluto` reference workspace has drifted out of sync again (EXP-025 fix reverted, never refixed)

**Description:** `examples/pluto/` is missing `test/test_schemas.ml`, `test/dune`, `.dockerignore`, and `.github/workflows/*`. Confirmed both by direct directory listing and by locating the exact revert commit `c0dfdb7a` ("Revert 'Merge branch 'EXP-025/pluto-workspace-sync'''", 2026-06-13) that deleted these five files immediately after EXP-025's merge (`33f18145`), following a test failure (`33baf66d`, "pipeline: test failure blocked EXP-025"). The ticket still sits in `project/tickets/DONE/EXP-025.md` describing a fix that no longer exists in `main`. The current `sol new workspace` scaffold template (`sol_cli_scaffold_templates.ml`) still generates all of these files correctly — only the checked-in reference example regressed. This is distinct from FRIC-014, which separately fixed and CI-guarded `examples/pluto`'s Dockerfiles specifically; that fix is still live and unaffected by this finding.

**Impact:** An AI agent or new engineer reading `examples/pluto/` as a reference for "what does a real Sol workspace look like" will omit the schema-compatibility test, the down-migration companion file, the CI workflow, and the Docker ignore file from anything they scaffold by hand or extend by analogy — undermining the Stage 9 UX-audit invariant that agents can work accurately from the generated structure alone.

**Remediation:** Regenerate the missing pieces in `examples/pluto/` from the current `sol new workspace` template output: `.dockerignore`, `.github/workflows/deploy.yml`, a CI workflow (name it `sol-ci.yml`, not the reverted attempt's pre-rename `sun-ci.yml`), `test/test_schemas.ml`, `test/dune`. This time, run the full test suite locally before merging to catch whatever caused the 2026-06-13 revert, since that failure was never diagnosed.

## Review — automated checks passed
Build clean, diff scoped to examples/pluto regeneration only, independently re-ran full suite (unit/kafka/e2e all pass), verified test_schemas.ml/dune correctly reference pluto's real Charged/pluto_payments_events (not placeholders), sol-ci.yml is a legitimate generic workspace-root template (no hardcoded paths needed), FRIC-014's Dockerfile fix untouched.
