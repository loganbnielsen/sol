---
id: BUG-030
type: bug
severity: medium
source: worker retry semantics review 2026-09-14, found while reviewing retry-topic naming
---

**Depends on:** None.

**Related:** FEAT-076, DEC-021.

DLQ scoping is resolved (2026-09-14): **per-group, locked** — see DEC-021's
amendment. This ticket is actionable.

Premise note: retry topic naming still appears to use the unscoped
`group_id ^ "-sol-retry"` consumer-group suffix; verify before implementation,
but do not use that buggy-state check as a `premise:` probe.

Retry and DLQ topic names are scoped to the source topic, but retry consumption is scoped
to the consumer group, so independent consumer groups can consume each other's retries.

## Problem

```text
source topic:      orders
retry topic:       orders-retry

consumer group A:  payments    → retry consumer group "payments-sol-retry"
consumer group B:  analytics   → retry consumer group "analytics-sol-retry"
```

Both retry consumer groups subscribe to `orders-retry`. A retry published on behalf of
group A is therefore delivered to group B's retry consumer, and group B will process a
retry for a message it never consumed (or never should have). The same shape applies to
the DLQ if it is source-scoped.

This violates consumer-group isolation and makes retry/DLQ traffic land in the wrong
application.

## Required behavior

Retry and DLQ topic identities must include the logical consuming group:

```text
<source-topic>.<group-id>.retry
<source-topic>.<group-id>.dlq
```

The exact encoding may differ, but it must:

- include both source-topic identity and consumer-group identity;
- produce valid Kafka topic names — note the 249-character limit and that mixing `.`
  and `_` produces Kafka metric-name collisions, so the group id must be sanitized
  and truncated (and hashed if truncation would collide);
- be deterministic;
- live in one canonical naming helper used by the producer, topic provisioning, the
  retry consumer, DLQ tooling, and documentation — never reconstructed ad hoc.

### DLQ scoping — resolved: per-group, locked (DEC-021, 2026-09-14)

Retry topics are group-scoped. **The DLQ is too, by the same reasoning, not merely for
consistency**: `Dead_letter` is the disposition of a *consumer group's processing
attempt*, not an intrinsic property of the source event — if `payments` cannot process
event X but `analytics` can, that is payments' dead letter, not a fleet-wide fact about
X. Retry and DLQ destinations both belong to the logical consumer group whose processing
responsibility they receive; one invariant, not two.

The operational-fragmentation cost (one dead-letter surface per group) is accepted
deliberately — it's a tooling-aggregation problem (a future `sol dlq list`/dashboard can
read across group-scoped topics), not a reason to physically merge dead letters and push
group identity into a header every producer/tool must preserve correctly instead.

**Every DLQ record still carries an `X-Sol-Origin-Group` header** naming the consumer
group that dead-lettered it — provenance that travels with the record even though
routing doesn't need it, so a record stays self-describing if exported or inspected
independently of its topic name.

## Invariant

> Retry and dead-letter destinations both belong to the logical consumer group whose
> processing responsibility they receive. A retry or DLQ record produced on behalf of
> consumer group A must only be consumable by retry/DLQ infrastructure belonging to
> consumer group A.

## Migration

Existing unscoped retry/DLQ topics may contain in-flight records. Migration must not
silently strand them: either drain legacy unscoped topics while routing new failures to
group-scoped topics, or explicitly assert that no legacy records exist before changing
naming. Do not rename topics in place assuming Kafka migrates their contents.

**Skipped deliberately.** Per `~/Code/CLAUDE.md`'s pre-alpha/no-backward-compatibility
policy, there are no real deployments and therefore no in-flight records on the old
unscoped `<topic>-retry`/`<topic>-dlq` topics to strand. Draining or asserting against
records that cannot exist would be unnecessary complexity for a fact that is already
true by construction.

## Completion notes (2026-09-14)

Implemented as two commits: a pure mechanical refactor (relay record + smart
constructors, replacing the primitive-heavy `publish_raw` callback and its
`?headers`/`rewrite_retry_headers:bool` signature) landed first, proving behavior
unchanged; this ticket's behavioral commit builds on it.

- Added `relay_topic_name ~source ~group_id ~suffix` in
  `kafka_service_retry_topics.ml` as the one canonical naming helper —
  `<source>.<canonical-group>.<retry|dlq>`. `canonical_group_segment` sanitizes
  `group_id` to alphanumerics/`-` (Kafka's metrics/JMX layer treats `.`/`_` as
  interchangeable) and, if the sanitized id would risk Kafka's 249-byte topic name
  limit, truncates it with a deterministic content-hash suffix so two different
  overlong ids can never collide. Exposed via both `.mli`s for direct unit testing.
- `consume`'s topic-name construction, the producer's `ensure_topic` provisioning
  calls, and the background retry consumer's subscription all derive their
  retry/DLQ topic names from this single helper — no ad hoc reconstruction anywhere.
  No separate DLQ tooling exists in the repo yet, so there was nothing else to update.
- `dead_letter_message` and `retry_decode_failure_message` now take `~group_id` and
  stamp an `X-Sol-Origin-Group` header (per DEC-021's amendment) — provenance that
  travels with the record even though routing no longer needs it.
- `execute_action` and `route_retry_decode_error` thread `~group_id` through to the
  DLQ path only; the retry-topic path needs no group_id parameter beyond what's
  already in the topic name.
- Tests: `relay_topic_name` covers canonical shape, sanitization of invalid
  characters, empty-group-id fallback, and deterministic truncation with distinct
  hash suffixes for two different overlong group ids. Existing dead-letter and
  retry-decode-error tests extended to assert `X-Sol-Origin-Group` appears in the
  published relay's headers.
- No demo/example update: this is an internal retry/DLQ addressing fix, not a
  change to any app-author-facing contract (`sol.toml`, CLI, generated manifests).

## Acceptance criteria

Given two independent consumer groups A and B on the same source topic:

- A retry produced by A appears only in A's retry topic; B does not consume it.
- A DLQ record produced by A appears only in A's DLQ destination.
- Producer, provisioning, retry consumer, DLQ tooling, and docs all use the same
  canonical naming helper.
- Group ids that are missing, empty, over-length, or contain `.`/`_` are handled by the
  helper without producing an invalid or colliding topic name.
- Every DLQ record carries an `X-Sol-Origin-Group` header naming the consumer group that
  dead-lettered it.
- Existing single-group behavior is otherwise unchanged.
