---
id: DEC-014
type: decision
severity: medium
source: DEC-011 discussion 2026-09-10 — the charging model is separate from the provider decision
---

**Depends on:** None (DEC-011 decided the provider tiers; this decides how usage is charged and bounded).

Decide how hosted usage is priced and — more urgently — **bounded**, so a customer's workload cannot generate an unbounded bill for Sol.

## Question

DEC-011 chose provider tiers, which fixes the per-customer cost **floor**. It says nothing about the **exposure**: egress, data processing, request volume and storage scale with the customer's traffic and are billed to Sol. A flat price covering unbounded usage means Sol absorbs the tail — the difference between a $200 month and a $200,000 one is a customer's runaway loop, not a pricing table. How is that bounded, and how is the variable part charged for?

## Options

1. **Flat price, hard platform quotas.** The customer cannot exceed their allotment at all. Simple and predictable; needs real quota enforcement, and turns "we are being throttled" into a support conversation.
2. **Platform fee covering the floor, plus metered pass-through at cost** (egress, requests, storage) with overage. Margin-safe and honest; requires per-tenant usage attribution and a billing pipeline.
3. **Capped allotments with alerting and overage, no hard stop.** Middle ground: the customer sees it coming, and the cap protects Sol.

## Decision criteria

- **Bounding is the priority, not pricing.** A worst case that is a *design property* — quotas, limit ranges, egress caps, budget alarms — beats a worst case that depends on the customer behaving.
- **You cannot price what you cannot measure.** Per-tenant usage attribution is a prerequisite for options 2 and 3, and for knowing margin per customer at all.
- **The cheap tier is more predictable, not just cheaper.** Provider egress allowances (e.g. 20 TB per node) remove most of the classic runaway, so the tiers differ in risk as well as price and the model can reflect that.
- **DEC-006 covers billing.** This should land as an input to it rather than becoming a second mechanism.
- It has to survive a customer whose service is genuinely popular — a model that punishes success hard enough drives them away is worse than a slightly thinner margin.

## Decision Required

Not yet decided.

**Acceptance criteria:**

- A decision names the charging model and states the worst-case exposure per customer under it.
- The enforcement mechanism is identified (quotas, limits, alarms, metering) and owned by a ticket.
- It states how per-tenant usage is attributed, or records that as the blocking prerequisite.
