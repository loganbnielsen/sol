# Sol Scaffold Quality Audit — 2026-09-08

**Template:** `docs/audits/SCAFFOLD_AUDIT.md`
**Workspace:** `/tmp/sol-scaffold-audit-2026-09-08/scaffold_audit`
**Previous report:** `project/audits/2026-06-12_scaffold_audit.md` (no open findings)

## Executable Run

| Step | Result |
|------|--------|
| `sol new workspace scaffold_audit` | PASS, 28 files |
| `dune build` | PASS, zero warnings/errors |
| `sol new svc payments/refund` | PASS |
| `sol new worker logistics/fulfillment` | PASS |
| `sol new fn billing/invoice` | PASS |
| `sol new event billing/payment_confirmed` | PASS |
| Final `dune build` | PASS, zero warnings/errors |

## Checklist Results

### 1. Workspace Scaffold — [x] pass
Compiles clean. `events/<team>/` and `app/<team>/<name>-<primitive>/` paths correct. `charge_svc` → Kafka → `notify_worker` crosses domains through the `Charged` event contract, not service internals. Libraries workspace-namespaced (`scaffold_audit_payments_charge_svc`, etc.). README uses only `sol` commands, no stale scripts. File count is minimal and legible.

### 2. Service Scaffold (`-svc`) — [ ] one open item (already tracked)
- Uses `Sol.Service.Make` (via `Service.run`): pass.
- Auth explicit on every route (`~auth:`Public` even on `/health`): pass.
- Typed responses (`Response.ok/json/bad_request/internal_error`), no naked exceptions escaping handlers: pass.
- Naming conventions (module/file/library names derive from workspace/domain/service): pass.
- Business logic locality (`handler.ml` is the obvious edit point, no infra manipulation): pass.
- **Metrics/tracing wired by the framework: FAIL for the generic `sol new svc` template.** `app/payments/refund_svc/bin/main.ml` calls `Service.run Handler.routes ~env ()` with no `Sol_obs.of_env` and no `~ot`. This is byte-for-byte the same gap already tracked as **EXP-027** (filed by the 2026-09-08 UX audit, currently in `READY_FOR_ENGINEERING/`) — confirmed independently here via direct scaffold generation and source read rather than re-filed under a new ID. The workspace's own initial service (`charge_svc`, generated by `ws_svc_bin_ml`) wires `Sol_obs` correctly and passes `~ot:obs`; only the generic `svc_bin_ml` template used by `sol new svc` lacks it. Worker and fn templates both wire `Sol_obs` correctly.

### 3. Worker Scaffold (`-worker`) — [x] pass
Uses `Sol.Worker.Make`; `bin/dune` includes `sol_obs`; `bin/main.ml` wires `Sol_obs.of_env` and passes `~ot`. Handler doc comment and shape confirm the framework acks only after `Ok ()` is returned, and `Error` skips the commit (no manual `ack()` call exists in the generated surface — correct per current framework contract, stronger than the checklist's literal wording). `group_id` (`logistics-fulfillment-worker`) is stable and workspace/domain/worker-derived. Stub `Message` module is clearly marked for replacement with a real event contract import.

### 4. Function Scaffold (`-fn`) — [x] pass
Uses `Sol.Fn.Make`; `bin/dune` includes `sol_obs`; wires `Sol_obs.of_env` and `~ot`. `run` returns `result`; `Error \`Signalled` and other errors are handled explicitly, no bare exceptions. No HTTP/worker lifecycle assumptions. CronJob YAML is synthesized by the CLI, not hand-written (no k8s files in scaffold output).

### 5. Event Scaffold — [x] pass
`events/billing/payment_confirmed.ml` lives under the publishing domain; compiles against the `MESSAGE` module type (used successfully as `(module Charged)` in `charge_svc`); topic name (`Kafka_service.topic_name_exn "billing-payment_confirmeds"`) includes workspace/domain identity; schema is inline, explicit JSON Schema; `test/test_schemas.ml` provides the compatibility check path (skips safely without `SCHEMA_REGISTRY_URL`, matching CI-safe defaults). No service-module imports.

### 6. Storage and Migration Scaffold — [x] pass
`0001_notifications.sql` / `.down.sql` are numbered, idempotent (`CREATE TABLE IF NOT EXISTS` / `DROP TABLE IF EXISTS`). Table name (`scaffold_audit_notifications`) is workspace-prefixed. `lib/notification.ml` storage helpers return `result` via `Pg_db.exec`/`Pg_db.collect`, no naked DB exceptions at the boundary. Column set matches the handler's insert/list usage exactly.

### 7. Deployment Metadata Scaffold — [x] pass
`sol.toml` files are minimal, commented-out-by-default, high-level (`[infra.scale]`, `[infra.env]`, `[infra.rollout]`). Dockerfiles run as `USER 65534` (non-root) in the runtime stage for every primitive (svc/worker/fn). No hand-written Kubernetes manifests anywhere in scaffold output. `sol/prod/aws/us-east-1.yml` placeholder is clearly marked as needing rename, no secrets present.

### 8. AI-Agent Working Surface — [x] pass
Edit points obvious (`handler.ml`, `<name>_worker.ml`, `<name>_fn.ml`, event files, migrations, `notification.ml`). Templates name dependencies explicitly (dune `libraries` stanzas list every collaborator). Event/storage shape changes create compile pressure (typed `Message`/`Notification` fields flow into typed handler signatures). No dynamic module loading or stringly-typed wiring. README documents build/run/deploy/status/migrate/rollback with `sol` commands only.

## Findings

### Previously tracked, re-confirmed here
- **EXP-027** (already `READY_FOR_ENGINEERING`, filed by ux-audit) — not re-filed. Independently reproduced in this pass via fresh `sol new svc` generation.

No new `SCAFFOLD-*` findings — every checklist item outside the already-tracked EXP-027 gap passed on direct verification (fresh scaffold generation + build + source read, not assumption).

No stale-`DONE` mismatches found for prior `SCAFFOLD-*` tickets: there were none to check (previous report had zero open findings, and no `SCAFFOLD-*` ticket exists anywhere in `project/tickets/`).

## Summary

| Category | Result |
|---|---|
| Compile | PASS (both builds, zero warnings/errors) |
| Domain Ownership | PASS |
| Runtime Semantics | PASS (worker/fn); **FAIL (svc)** — tracked as EXP-027 |
| Security | PASS |
| Docs | PASS |
| AI-Agent Surface | PASS |
| New tickets filed | 0 (finding already tracked as EXP-027) |
| Previously-open findings resolved | 0 (none were open) |
