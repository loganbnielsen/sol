# Dogfood Run — 2026-09-07 (round 2)

Engineer: Claude (autonomous dogfood fork, verifying FRIC-006/007/008)
Machine/OS: Linux (WSL2)
Sol commit: 34ae9b00 (main, includes FRIC-006, FRIC-007, FRIC-008)

## Purpose

Re-run the golden path from scratch, on a genuinely fresh workspace, to check
whether the three fixes merged after the first 2026-09-07 run
(`RUN_2026-09-07.md`) actually resolved the golden path end to end:

- **FRIC-006** — `sol dev up`/`sol up` swallowed subprocess stdout/stderr on
  failure.
- **FRIC-007** — every generated service crash-looped permanently: the pinned
  Redpanda version predated JSON Schema Registry support.
- **FRIC-008** — a pre-Sun→Sol-rename `sun-local` k3d cluster silently
  port-conflicted with `sol-local`.

This run reused the existing, already-healthy `sol-local` k3d cluster from
this session's fix-verification work (post-FRIC-007 upgrade) rather than
tearing it down and rebuilding from a bare substrate — so this run validates
the **existing-cluster** path and the **fresh-workspace** path, not a
fresh-cluster bootstrap (already timed and validated separately during
FRIC-007's own verification).

## Tool versions

Docker: 29.5.3
k3d: v5.6.0
kubectl (client): v1.34.1
helm: v3.21.0+ge0878d4
dune: 3.23.1
OCaml: 5.4.0 (toplevel-reported; switch is 5.4.1 per repo convention)

## Timings

| Step | Elapsed |
|------|---------|
| sol new workspace | 0.02s |
| dune build | 2.56s |
| sol dev up, fresh cluster | not exercised this run (see FRIC-007's own verification: ~5min, unchanged, expected substrate-bootstrap cost) |
| sol dev up, existing cluster | 26.18s |
| sol up | 25.19s |
| sol migrate | 0.67s |
| first successful curl | ~12ms (after the above steps completed) |

**Total, `sol new workspace` through first successful `/health`, on an
existing substrate: ~54.6s** — comfortably under the two-minute claim.

Did the flow complete without manual intervention? **Yes.**
Could a new engineer understand the failure messages? **N/A — no failures occurred.**

## Friction Log

None. Every step succeeded on the first attempt, with no workaround, no
undocumented step, and no confusing output.

One informational (non-friction) message worth noting: `sol up` printed
`[sol up] replacing stale port-forward for dogfood-2026-09-07-payments/charge-svc on port 8080`
— this refers to a port-forward left over from the *previous* dogfood
workspace (manually exercised earlier this session while verifying
FRIC-006/007), not a new bug. The CLI correctly detected and replaced it
rather than silently double-binding or failing — this is `sol up`'s existing
stale-port-forward detection working as designed, mentioned here only
because it appeared in the transcript, not as a finding.

## Findings

**FRIC-007 confirmed resolved on a genuinely fresh workspace, not just the
manually-poked-at one.** The previous run's crash-loop was 100% reproducible
on a completely clean scaffold; this run's `charge-svc` and `notify-worker`
both rolled out successfully on the first `sol up`, with no manual
intervention, no `helm upgrade` retry, no schema-registry error anywhere in
the logs.

**FRIC-006 confirmed non-regressive.** No failures occurred to exercise the
improved error-surfacing paths directly in this run, but nothing about the
refactor (surfaced stderr, `helm_install ~label`, rollout diagnosis reuse)
introduced any new friction on the success path — output remained clean and
readable throughout.

**FRIC-008 not directly exercised** (no pre-rename `sun-local` cluster exists
on this machine anymore — it was never actually present here, that finding
was specific to a different dogfood machine per the original ticket). Not a
gap in this run; that check only matters for engineers with pre-rename local
state.

**Full DOGFOOD.md success bar met**, all nine criteria: generated workspace
builds, local substrate healthy, `sol up` deploys all services, `sol migrate`
applies migrations, `sol status` shows ready pods + reachable URL, `curl
/health` succeeds (200 "ok"), `POST /charges` publishes a `Charged` event
(202, `accepted: true`), `notify_worker` consumes it, `GET /notifications`
shows the worker-written row with correct data.

## Tickets filed

None. This run found no new friction to file — a clean pass, confirming the
three FRIC-006/007/008 fixes hold on a genuinely fresh workspace.
