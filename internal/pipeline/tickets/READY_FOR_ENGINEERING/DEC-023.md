---
id: DEC-023
type: decision
severity: medium
title: "Distribution and install story for @sol/* (revisits DEC-013)"
source: DEC-022 (TypeScript as the adoption funnel) — revisits DEC-013's
  "reserve the npm names now, publish when the product is worth sharing"
---

**Depends on:** DEC-022.

**Related:** DEC-012, DEC-013, DEC-025, FEAT-080, FEAT-082, FEAT-085.

**Follow-on:** DEC-025. This decision answered the TypeScript half of the
workspace-independence boundary. Completing the cutover immediately exposed the
identical boundary on the OCaml side — a workspace cannot build without a Sol
source checkout — which is DEC-025's question. The parallelism is deliberate and
worth preserving: whatever mechanism DEC-025 settles on should keep the same
source-vs-distribution split this decision made.

**Question:** What is the normal install/distribution story for the
`@sol/*` TypeScript packages, so that the TypeScript golden path
(FEAT-082) can actually be walked by someone outside this repository?

## Context

DEC-013 decided to reserve the npm scope now and publish later, because
distribution triggers dependency-licence obligations (INFRA-007) and no
external consumer existed. DEC-022 changes that premise: TypeScript is now
the adoption funnel, which makes distribution load-bearing rather than
optional. Today the intended experience is contradicted:

```text
README:       TypeScript is first-class!
developer:    npm install @sol/kafka
npm:          404
```

The packages are real enough to be published (built, tested, dogfooded by
`demo_ts`, CI-covered) — but they remain `"private": true` and unpublished,
so a TS developer can only consume them by cloning this repo. That is not a
golden path.

## Options

1. **Publish `@sol/kafka`/`@sol/obs` to npm** (claim the scope), gated on
   FEAT-082's golden path being coherent and INFRA-007's licence inventory
   being done.
2. **Document an in-repo install path** (npm workspace / git dependency)
   until the golden path and licence inventory are ready, so an early user
   can still get started without cloning-by-hand guesswork.
3. Keep source-only, as today.

## Decision (2026-09-15) — Option 2, with npm as the explicit target

**Chosen:** document a working interim install path *now* (option 2), and
publish the supported `@sol/*` packages to npm once **FEAT-082 establishes a
coherent TypeScript golden path** *and* **INFRA-007 clears their distribution
dependencies**. Option 3 (source-only) is rejected: DEC-022 makes TypeScript the
adoption funnel, so the README's "first-class" claim cannot sit next to a 404.

The trigger is deliberately a *destination*, not an open-ended reconsideration —
npm is where this ends up. Using an interim mechanism today does not weaken
DEC-022; it acknowledges that the product is not yet at its external-release
point:

```text
today   TS = first-class product direction   distribution = interim/dev path
target  TS = first-class usable product      distribution = normal npm packages
```

**Reconciles with DEC-013** (reserve the scope, publish when the product is
worth sharing): its two stated reasons for deferring were dependency-licence
obligations and the absence of an external consumer. The second no longer holds
(DEC-022), and the first is precisely INFRA-007's job — hence the gate rather
than a bypass.

**INFRA-007** remains the blocking prerequisite for the *publish* step; its
dependency-licence audit is not assumed away here.

## Resolution detail (2026-09-15): the scope is `@sol-fab`

The `sol-fab` npm organization now exists. That settles *where* the packages
would be published, and it surfaces a naming consequence the original options
did not name:

- npm scopes are org-owned, so the publishable identifiers are
  **`@sol-fab/kafka`** and **`@sol-fab/obs`**. **`@sol/*` cannot be published**
  unless the `sol` org is owned — so today's in-repo names
  (`packages/sol-kafka` → `@sol/kafka`, `packages/sol-obs` → `@sol/obs`) are
  *internal* names, not the public ones.
- Publishing under `@sol-fab/*` therefore requires either renaming the packages
  (plus every import, the demo's dependencies, and the README/CLAUDE.md
  references), or keeping the internal names and telling consumers to alias
  (`"@sol/kafka": "npm:@sol-fab/kafka@^0.1.0"`). Aliasing works, but it is a
  poor first impression for a path whose whole point is a clean DX.

**Recommendation:** adopt `@sol-fab/*` in-repo *before* the publish step, so the
scaffold (FEAT-084), the docs, and the published artifact all agree. Deferring
the rename to publish time would instead churn the scaffold and the README once
the golden path exists. Mechanically it is a rename of the two workspace
packages plus their references — cheaper now than later.

## Remaining work

The decision is made; what remains is the interim path itself — README's
TypeScript section must point at an install mechanism that actually works for an
external developer (verified, not sketched), and the publish step's licence
obligation is discharged via INFRA-007.

## Sequencing corrected by FEAT-082's resume (2026-09-15)

The original plan published `@sol-fab/*` only *after* FEAT-082 established a
coherent golden path. The resumed walk (post-BUG-034 / PR #271) falsified that
ordering: FEAT-082 cannot represent the external-developer path while the only
route to `@sol-fab/kafka`/`@sol-fab/obs` is sitting inside Sol's own source
repository (the fixture's Dockerfile `COPY packages/sol-kafka ...`). The interim
install path is therefore **on the critical path** for FEAT-082, not downstream
of it.

The interim mechanism must have workspace-scoped semantics: a Sol workspace's
`package.json` obtains `@sol-fab/kafka`/`@sol-fab/obs` through normal dependency
resolution, and the Docker build context needs only files inside the workspace
plus that resolution. An "in-repo npm workspace / git dependency" path that
requires the enclosing Sol checkout does **not** satisfy this — that is exactly
the monorepo coupling DEC-024 removes. Options to evaluate (deliberately not
decided here): standalone git repositories per the kafka-eio/obs-eio precedent,
a release-tarball URL, or an interim registry. Public npm remains the DEC-023
target once INFRA-007 clears.

## Decision (2026-09-15, revised) — standalone GitHub repos + npm

The mechanism is decided: each existing capability lives in its own public
GitHub repository under the personal account, is independently
buildable/testable/publishable, and is consumed from npm under `@sol-fab/*`.
GitHub is the source/repository boundary; npm is the normal distribution
boundary. This follows the kafka-eio/obs-eio extraction precedent.

Explicitly rejected: `vendor/ts`, monorepo-relative `file:` dependencies,
`../../packages/...`, widening the Docker build context back to the enclosing
repo, and workspace-root inference from `package.json`/`dune-project`.

## Progress (2026-09-15)

- `loganbnielsen/sol-obs` → npm `@sol-fab/obs` — created and pushed; CI green
  (build + 12 tests). Apache-2.0; `package.json`/exports/tsconfig/tests/README/
  CI/tag-driven release workflow.
- `loganbnielsen/sol-kafka` → npm `@sol-fab/kafka` — created and pushed; builds
  and 42 unit tests pass (3 broker-backed tests self-skip). Depends on
  `@sol-fab/obs@^0.1.0`; its CI was red for a reason that was *misdiagnosed* —
  see "Workflow readiness corrected" below.
- INFRA-007's npm dependency-licence inventory for the distributed trees is
  recorded in `docs/legal/third-party-licenses.md` (all Apache-2.0/MIT; no
  copyleft shipped).
- **Blocker: no npm publish credentials.** This environment has no `~/.npmrc`,
  no token in the environment, and no `NPM_TOKEN` secret on either new repo;
  `npm whoami` → `ENEEDAUTH`. `npm publish --dry-run` is clean, so publication
  is ready once a token is supplied — add `NPM_TOKEN` to both repos and tag
  `v0.1.0` (sol-obs first, then sol-kafka), or `npm login`.
- The Sol monorepo still carries `packages/sol-obs`/`packages/sol-kafka` and the
  demo still imports `@sol/*`. They are removed and repointed only after the
  published packages exist; removing first would break the monorepo with no
  replacement. FEAT-085 and the FEAT-082 resume follow that cutover.

## Workflow readiness corrected (2026-09-15, later)

The "publication is ready once a token is supplied" claim above was wrong, and so
was the diagnosis of sol-kafka's red CI. Both are corrected here. The credential
blocker itself still stands.

**sol-kafka's CI was not red because `@sol-fab/obs` is unpublished.** All three
`actions/setup-node` steps in that repo passed `cache: npm` while the repo has no
committed lockfile, and setup-node fails hard in that case:

```text
##[error]Dependencies lock file is not found in /home/runner/work/sol-kafka/sol-kafka.
Supported file patterns: package-lock.json,npm-shrinkwrap.json,yarn.lock
```

Both CI jobs died *inside* setup-node, ~11 s in, before `npm install` ever ran —
so the unpublished dependency was never reached. With caching removed the
workflow proceeds to the real, expected failure (`E404 @sol-fab/obs`), which
resolves itself the moment obs ships.

**Trusted publishing cannot bootstrap either package.** A trusted publisher is
attached to a package that already exists on the registry — npm's own `npm trust`
prerequisites state "Package must exist". Both `@sol-fab/obs` and
`@sol-fab/kafka` 404, so **v0.1.0 cannot be published via OIDC.** The shape is a
one-time bootstrap credential, then OIDC for every release after it:

```text
v0.1.0            bootstrap token (NPM_TOKEN, short-lived, then deleted)
v0.1.1 onward     npm trust github <pkg> --file release.yml --allow-publish
```

**Version floor.** Every Node 22 release ships npm 10.9.8. OIDC trusted
publishing requires npm >= 11.5.1 and `npm trust` requires >= 11.15.0, so the
Node 22 pin in these workflows could not have authenticated that way at all.
Both repos now pin `node-version: 24` (npm 11.19.x).

An `NPM_TOKEN` and a `trust` configuration conflict, so the bootstrap token must
be removed from both release workflows (and both repo secrets deleted) at the
flip. That instruction is recorded in the workflow headers themselves, next to
the `env:` block it refers to.

### State at time of writing

- Done and pushed, credential-free: `node-version: 24` across all four workflows;
  sol-kafka's `cache: npm` removed (to be restored together with `npm ci` once the
  lockfile exists); the bootstrap→OIDC path documented in both `release.yml`.
  Commits `sol-obs@3b083ee`, `sol-kafka@520db9f`. sol-obs CI green on Node 24;
  sol-kafka CI red only at `npm install` (`E404 @sol-fab/obs`) — the honest state.
- The blocker is now genuinely *just* the credential. Supply a bootstrap
  `NPM_TOKEN` for both repos: a granular token with publish rights on `@sol-fab`.
  The 2FA-bypass option is **correct here** — it is what lets CI publish
  unattended. Tag `v0.1.0` obs-first, then sol-kafka. Then run `npm trust github`
  for each package, which is a *separate* credential path: `npm trust` rejects
  granular tokens carrying the 2FA-bypass option, so it must run locally with
  interactive 2FA. Do not reuse the bootstrap token's properties for it.
- The flip needs local npm >= 11.15.0 for `npm trust`; this environment has
  10.9.8 (Node 22.23.0), so local npm must be upgraded before the flip.

## Bootstrap attempt (2026-09-15) — blocked on 2FA mode, not on the token

Tagged `v0.1.0` on sol-obs (`3b083ee`); the Release workflow ran and got all the
way to the publish step. `npm ci`, build, test, tarball assembly (11 files,
9.4 kB) and **Sigstore provenance signing** all succeeded. Then:

```text
npm notice npm tokens that bypass 2FA are being restricted for account changes and direct publishing.
npm error code E403
npm error 403 Forbidden - PUT https://registry.npmjs.org/@sol-fab%2fobs
npm error 403 - Two-factor authentication or granular access token with bypass 2fa enabled is required to publish packages.
```

Nothing was published (`@sol-fab/obs` is still 404), so the tag is unchanged and
that run can simply be re-run once the credential satisfies this. The failure is
narrow: the token authenticates, but this account requires 2FA for writes and the
token does not carry the 2FA bypass.

### The bootstrap token and the flip want opposite things

- **v0.1.0 (bootstrap)** needs a token that can publish unattended → 2FA bypass.
- **The flip** (`npm trust github`) explicitly rejects 2FA-bypass granular tokens
  and now requires an interactive 2FA challenge.

A normal (non-bypass) granular token therefore cannot publish under this
account's 2FA mode at all.

### Where npm is heading (the bypass path is being retired)

From GitHub's changelog of 2026-07-31, fetched verbatim:

- **In effect now:** 2FA-bypass tokens can no longer create/delete tokens, change
  package access or maintainers, manage org membership, or **configure trusted
  publishing** — those need an interactive 2FA challenge.
- **Targeted January 2027:** 2FA-bypass tokens **lose direct publish** too; their
  publishing surface reduces to reading private packages and *staging* a publish
  for a maintainer to approve with 2FA. GitHub's stated migration path is
  "trusted publishing (OIDC) or staged publishing".

So OIDC is not leisure-time cleanup — automated publishing on a bypass token has
a ~4-month horizon. Source:
`https://github.blog/changelog/2026-07-31-restricting-npm-bypass-2fa-granular-access-tokens/`

Secondary summaries disagree with each other about whether bypass-2FA tokens can
still be *created* (claims range from "deprecated July 2025" to "revoked February
2026"). npm 12's own shipped docs settle it: `npm token create` still documents a
`bypass-2fa` flag and describes it as "useful for automation and CI/CD
workflows". Those summaries are wrong — creation still exists. Resolved below.

### Staged publishing cannot bootstrap either (rules out the obvious fix)

`npm stage` docs ship with a `**Package must exist**` prerequisite, identical to
trusted publishing's. So the two automated paths npm recommends for retiring
bypass tokens — OIDC and `npm stage publish` — **both** require the package to
already exist, and neither can create `@sol-fab/obs`. npm's guidance to "delete
all your tokens with bypass enabled and switch to a trust relationship… or
create a GAT without bypass and use `npm stage publish`" has a gap for a
brand-new package: the first publish has no automated route at all.

That leaves exactly two viable bootstrap paths, both needing a human once:

1. **Bypass-2FA granular token.** Still creatable, and scriptable rather than
   web-UI-only — so this does not depend on the UI still showing the toggle:
   ```bash
   npm login --auth-type=web          # interactive 2FA
   npm token create --bypass-2fa \
     --scopes @sol-fab \
     --packages-and-scopes-permission read-write \
     --expires 7 \
     --name "sol bootstrap publish"
   ```
   `--scopes` (not `--packages`) is the load-bearing flag: neither package exists
   yet, so a package-scoped token could not select them. `--expires` is in days.
   Put the value in `NPM_TOKEN` on both repos and re-run the existing `v0.1.0`;
   provenance is preserved and no new tag is needed. Delete the token at the flip.

   **Local npm must be upgraded first.** npm 10.9.8 (this environment's) does ship
   `npm token create`, but its entire flag set is `[--read-only] [--cidr=list]` —
   legacy-token shaped. The granular flags (`--bypass-2fa`, `--scopes`,
   `--packages-and-scopes-permission`, `--expires`) exist only in npm 11+. The
   same upgrade is needed for `npm trust` (>= 11.15.0), so one
   `npm install -g npm@latest` covers both.
2. **Interactive local publish**: `npm login --auth-type=web` then
   `npm publish --otp=<code>` with local npm >= 11.15.0. No long-lived credential,
   but v0.1.0 carries **no provenance** (a local publish cannot obtain a Sigstore
   statement) and it needs a human at a terminal.

### Hardening to apply at the flip, not before

npm recommends setting each package's access control to *"Require two-factor
authentication and disallow tokens"*, and configuring any trust relationship with
`--allow-stage-publish` only. Both are post-bootstrap actions: they require the
package to exist, and applying them to a package that is not yet published is not
possible. Note also that trust relationships support `--allow-publish` and
`--allow-stage-publish` separately, so the flip has a choice worth making
deliberately rather than by default.

### Correction to an earlier claim in this ticket

The note above says the registry "only supports one [trusted publisher]
configuration per package". That came from the `npm trust` docs shipped in npm
11.19.1, but GitHub's changelog of 2026-09-03 announces "Multiple trusted
publishing configurations for npm". The one-configuration limit no longer holds;
`npm trust list` remains the way to inspect what is configured.

Minor, unrelated: `actions/setup-node` writes `always-auth` into `.npmrc`, which
npm 11 now warns about ("Unknown user config"). Cosmetic today; slated to break
in the next npm major.

## Published (2026-09-16) — both packages are live

### Result
- **`@sol-fab/obs@0.1.0`** — published by the Release workflow from tag `v0.1.0`
  (sol-obs `3b083ee`). Apache-2.0, 11 files, Sigstore provenance attested.
- **`@sol-fab/kafka@0.1.0`** — published from tag `v0.1.0` (sol-kafka `6b09761`).
  Apache-2.0, provenance attested, depends on `@sol-fab/obs@^0.1.0`.
- The bootstrap credential worked on the second attempt, after the 403 above;
  nothing about the publish pipeline itself needed changing, only the token's 2FA
  mode.

### Verified end to end, not merely "green"
- **sol-kafka CI is green for the first time**, and the broker job ran the
  Retry/DLQ ownership-transfer tests against real Redpanda: **45 tests, 45 pass,
  0 skipped**. The same suite reports 42 pass / 3 skip locally, so the three
  broker-backed tests genuinely execute in CI. Their passing was previously
  asserted; it is now demonstrated.
- The committed lockfile pins `@sol-fab/obs@0.1.0` with integrity
  `sha512-DkTCv34QUkvk8dUkB7DVQebKLaNoIoL98UCcUf+oB4qGUvtmY3Tfx51PkVAP2ysXZ8i+tqrGnawMAsS6nd3ozA==`,
  matching the published registry metadata — the artifact CI built is the
  artifact consumers install.
- A clean directory **outside any Sol checkout** installed `@sol-fab/kafka@^0.1.0`,
  resolved `@sol-fab/obs@0.1.0` transitively from the registry, imported both
  packages and executed them. That is the DEC-024 boundary criterion demonstrated
  in miniature, ahead of the FEAT-082 walk it is meant to serve.

### Operational gotcha: first-publish packument lag
After each first-ever publish the **version metadata and tarball were immediately
public while the packument 404'd** — so `npm view` / `npm install` failed even
though `.../obs/0.1.0` and the tarball URL both returned 200. Lasted ~5.5 min for
obs and ~4.5 min for kafka. A control package resolved normally throughout, so
this was npm's first-publish indexing, not a visibility or hold problem. **Do not
diagnose an apparently-failed first publish from the packument alone.**

### Remaining work, in order
1. **The OIDC flip — the deadline item.** Run `npm trust github` for each package
   (needs local npm >= 11.15.0 and interactive 2FA), then delete `NPM_TOKEN` from
   both repos, remove the `env: NODE_AUTH_TOKEN` blocks from both release
   workflows, and delete the bootstrap token itself. Leaving a bypass token in
   place is not durable: npm targets January 2027 for removing direct publish from
   them.
2. **Package-level hardening, now possible because the packages exist:** set each
   package's access control to "Require two-factor authentication and disallow
   tokens", and choose the trust relationship's permissions deliberately
   (`--allow-publish` vs `--allow-stage-publish`).
3. **The Sol monorepo cutover** — remove `packages/sol-obs`/`packages/sol-kafka`
   and repoint `demo_ts` and the ts-tests at `@sol-fab/*`. Unblocked as of now;
   this was deliberately not done earlier because removing the in-tree packages
   before the published replacements existed would have broken the monorepo.
4. FEAT-085, the `/tmp/foo` DEC-024 proof, then the FEAT-082 walk resumes.

### Bootstrap cost, stated plainly
The one-time credential was a granular token with 2FA bypass, scoped to the
`@sol-fab` **org** rather than to packages — neither package existed to be
selected. It is bootstrap-only and must be deleted at step 1. Note also that it
could **not** have configured the trust relationship itself: npm now requires an
interactive 2FA challenge for trusted-publishing changes.

## Acceptance criteria

- A written decision names the distribution mechanism and its trigger.
- README's TypeScript section points at a path that actually works for an
  external developer.
- Any publish step's licence obligation is addressed via INFRA-007, not
  assumed away.
- A Sol workspace can obtain `@sol-fab/kafka`/`@sol-fab/obs` from outside the
  Sol repository (the FEAT-082 boundary criterion), without widening the
  workspace's Docker build context.

