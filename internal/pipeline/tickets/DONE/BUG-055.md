---
id: BUG-055
type: bug
severity: low
source: internal/pipeline/audits/2026-09-24_correctness_audit_pass2.md
---

`Kafka_service.config_of_env`: require `KAFKA_BROKERS`, `SCHEMA_REGISTRY_URL` and `REDPANDA_ADMIN_URL` instead of defaulting to localhost

**Depends on:** SEC-007.

**Finding:** FND-0054 (`internal/pipeline/audits/findings/`).

**Premise verified 2026-09-24** against `origin/main @ fd5c7e0c`
(`kafka_service_config.ml:2-25` defaults all three).

## Problem

An unset address becomes localhost. In a pod nothing listens there, so an overlay or
external-substrate config that omits one fails later, with an error naming localhost
instead of the missing variable.

## Remediation

Return an `Error` naming each missing variable, as SEC-007 did for
`KAFKA_SECURITY_PROTOCOL`. Update everything that relies on the defaults: fixture and
example run commands, `run_tests.sh`, the e2e skill, and the tutorial's local tables.
`sol local run` and rendered manifests already set all three.

## Acceptance criteria

- Test: each unset variable is an `Error` naming it.
- Documented local commands set all three.
- Demo/example: the local demo and example run commands set them.
- TS parity: record the verdict (DEC-022).

## Completion notes

- `Kafka_service_config.of_env` requires `KAFKA_BROKERS`, `SCHEMA_REGISTRY_URL` and
  `REDPANDA_ADMIN_URL`. An unset or blank one is an `Error` naming **every** missing
  variable, with an example local setting; nothing defaults to localhost. `.mli` and
  `kafka-eio-service.md` updated.
- Tests (`test_kafka_service.ml`): each variable unset alone is an error naming it; two
  unset are named together; all three stated is accepted. A `with_kafka_env` helper
  states a complete local environment, so SEC-007's protocol tests isolate their one
  variable. Mutation check (`audits/README.md`): short-circuiting the address check to
  `Ok ()` builds (rc=0) and fails "addresses are required".
- Callers that relied on the defaults now state all three: AGENTS.md (test and demo
  commands), `run_tests.sh` (kafka and e2e), `setup-local.sh`, the e2e skill,
  `internal/pipeline/audits/AUDIT.md`, the local-demo, retry-demo and venus run comments, and
  `examples/pluto/README.md`. `cli/platform/local/k8s/{svc-template,demo-app}.yaml`
  were missing `REDPANDA_ADMIN_URL` entirely and now set it. The tutorial's local and
  in-cluster tables list all three. Rendered manifests and `sol local run` already set
  them.
- Demo/example: the pluto TS demo required no address before (`?? "localhost:…"` in its
  own code). Both workloads now fail at startup naming the missing variable; `npm run
  build --workspaces` passes.
- Language parity (DEC-022): implemented for the TS demo in this PR. `@sol-fab/kafka`
  has no `config_of_env` equivalent (the app reads the environment), so there is no
  library change. The transport-posture half stays FEAT-097.
