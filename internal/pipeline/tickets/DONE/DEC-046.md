---
id: DEC-046
type: decision
severity: medium
title: Adopt the repository organization proposal — audience-split top level, code vs assets, shared/local/cloud platform, provider-mirrored roots
source: operator review notes (2026-09-25), internal/pipeline/audits/2026-09-25_organization_proposal.md
---

**Depends on:** None.

**Proposal:** `internal/pipeline/audits/2026-09-25_organization_proposal.md`. The proposal is authoritative for the rules, the target layout and the move table; this ticket records the decision.

## Decision

**Decided 2026-09-25 by the operator.** The proposal was reviewed in two external rounds on #515; its **Review log** records what changed. The six rules and the target layout are accepted as amended there. Open questions:

1. **`docs/planning/` is split.** Public direction stays public; maintainer bookkeeping moves to `internal/`.
   - `ROADMAP.md` → `docs/ROADMAP.md`.
   - `WORK_SUMMARY.md`, `OPAM_FOUNDATION_TRACKER.md` and `LIVE_DEV_DEPLOY_ROADMAP.md` → `internal/planning/`.
   - `docs/planning/` goes away.
2. **`docs/architecture/` stays in `docs/`.** ADRs and design notes explain Sol to evaluators and users as well as maintainers. The exception is `contributing-map.md`, which only contributors need; it moves to `internal/contributing-map.md`.
3. **The rule-6 mechanism is as proposed.** REFAC-104 produces the dependency graph. A domain the graph separates cleanly becomes its own `(wrapped false)` dune library under `(include_subdirs no)`, and anything caught in a cycle stays in the top-level library, with the cycle named.

The moves in answers 1 and 2 are DOCS-023's, which already carries "apply DEC-046's answers".

## Acceptance criteria

- The decision and the answers to open questions 1–3 are recorded here, and the proposal is updated to match.
- `AGENTS.md`'s ticket-frontmatter line lists the ticket `type` values actually in use. Today it lists `ux-finding | audit-finding | feature | bug`, while the tree also uses `decision`, `refactor`, `docs-finding`, `dogfood-finding`, `code-layer-finding`, `verification` and more. Derive the list with `rg --no-filename '^type:' internal/pipeline/tickets | sort | uniq -c` (not `rg -h`, which in ripgrep prints help).
- `AGENTS.md`'s repo-layout section and `internal/README.md` state the six rules. Rules, not just the tree: the tree will change, and the rules are how a new file finds its home.

## Completion notes

- **Recorded:** the decision above; the proposal's status, open questions and target layout are updated to match.
- **`AGENTS.md`:** the ticket-frontmatter line now lists the 14 `type` values in use. `rg --no-filename '^type:' internal/pipeline/tickets | sort | uniq -c` gave, on `12f495e9`: refactor 168, feature 160, bug 142, audit-finding 89, decision 46, ux-finding 34, dogfood-finding 29, docs-finding 20, code-layer-finding 14, verification 10, release 3, infra 2, performance 1, documentation 1.
- **The rules are stated** in `AGENTS.md` (a new *Organization rules* section above *Repo layout*) and in `internal/README.md`. The *Repo layout* tree still shows today's tree, with a note that it changes as the implementation tickets land; each move ticket updates it.
- **Demo/example:** not applicable; this is a decision record.
- **Language parity (DEC-022):** rule 5 gives TypeScript a `framework/` slot (DOCS-024). No runtime impact.
