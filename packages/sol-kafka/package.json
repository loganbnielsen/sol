{
  "name": "@sol/kafka",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Sol's Kafka policy layer on top of kafkajs -- schema registry conventions, explicit topic provisioning, decode/retry/crash routing, and W3C traceparent propagation. Not a Kafka client; kafkajs remains the transport.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "peerDependencies": {
    "kafkajs": "^2.2.4",
    "@opentelemetry/api": "^1.9.0"
  },
  "devDependencies": {
    "@opentelemetry/api": "^1.9.0",
    "@types/node": "^22.9.0",
    "kafkajs": "^2.2.4",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
