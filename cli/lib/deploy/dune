; REFAC-104: plans, releases, rollout, rollback and the deploy-time checks.
; Domains form a DAG (base <- kube <- workspace <- cloud <- deploy; local -> workspace),
; so a module can only use another domain listed in (libraries) below.

(library
 (name sol_cli_deploy)
 (wrapped false)
 (libraries
  sol_cli_base
  sol_cli_kube
  sol_cli_workspace
  sol_cli_cloud
  unix
  yojson))
