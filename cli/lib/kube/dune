; REFAC-104: cluster access: kubectl, port-forwards, helm, cluster identity.
; Domains form a DAG (base <- kube <- workspace <- cloud <- deploy; local -> workspace),
; so a module can only use another domain listed in (libraries) below.

(library
 (name sol_cli_kube)
 (wrapped false)
 (libraries sol_cli_base unix yojson ptime))
