; REFAC-104: the workspace model: sol.yml/sol.toml config, manifests, `sol new` scaffolding.
; Domains form a DAG (base <- kube <- workspace <- cloud <- deploy; local -> workspace),
; so a module can only use another domain listed in (libraries) below.

(library
 (name sol_cli_workspace)
 (wrapped false)
 (libraries sol_cli_base sol_cli_kube cmdliner otoml unix yaml))
